# input CreateTransactionInput { amount: Float, contactId: ID!, currency: String, listingId: ID!, status: String, subtitle: String, title: String! }
mutation TransactionCreate($input: CreateTransactionInput!) {
    createTransaction(input: $input) {
        ...TransactionDetails
    }
}
