# input UpdateTransactionInput { amount: Float, contactId: ID, currency: String, listingId: ID, status: String, subtitle: String, title: String!, version: Long! }
mutation TransactionUpdate($id: ID!, $input: UpdateTransactionInput!) {
    updateTransaction(id: $id, input: $input) {
        ...TransactionDetails
    }
}
