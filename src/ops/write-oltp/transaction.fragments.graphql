# type Transaction {
#   amount: Float, contactId: ID!, currency: String, id: ID!, listingId: ID!,
#   status: String, subtitle: String, title: String!, version: Long!
# }

fragment TransactionListFields on Transaction {
    id
    title
    status
    total { amount currency }
}

fragment TransactionDetails on Transaction {
    id
    title
    subtitle
    status
    total { amount currency }
    contactId
    listingId
    version
}
