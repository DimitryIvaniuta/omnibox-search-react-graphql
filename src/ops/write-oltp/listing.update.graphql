# input UpdateListingInput { contactId: ID, subtitle: String, title: String!, version: Long! }
mutation ListingUpdate($id: ID!, $input: UpdateListingInput!) {
    updateListing(id: $id, input: $input) {
        ...ListingDetails
    }
}
