# input CreateContactInput { email: String!, fullName: String!, label: String, phone: String }
mutation Contact_Create($input: CreateContactInput!) {
    createContact(input: $input) {
        ...ContactDetails
    }
}